{
    "version": 2,
    "framework": "nextjs",
    "buildCommand": "npx prisma@5.22.0 generate --schema packages/db/prisma/schema.prisma && cd apps/app && npm run build",
    "outputDirectory": "apps/app/.next",
    "installCommand": "npm install"
}
