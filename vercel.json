{
    "version": 2,
    "framework": "nextjs",
    "buildCommand": "DATABASE_URL=${DATABASE_URL:-postgresql://postgres:postgres@127.0.0.1:5432/postgres} DIRECT_URL=${DIRECT_URL:-postgresql://postgres:postgres@127.0.0.1:5432/postgres} npx prisma@5.22.0 generate --schema packages/db/prisma/schema.prisma && cd apps/app && npm run build",
    "outputDirectory": "apps/app/.next",
    "installCommand": "npm install"
}
