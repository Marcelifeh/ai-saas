{
    "version": 2,
    "framework": "nextjs",
    "buildCommand": "npx prisma generate --schema packages/db/prisma/schema.prisma && cd apps/app && npm run build",
    "outputDirectory": "apps/app/.next",
    "installCommand": "npm install"
}
